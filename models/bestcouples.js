class Post {
  constructor(
    id,
    userId,
    userName,
    imageUri
  ) {
    this.id = id;
    this.userId = userId;
    this.userName = userName;
    this.imageUri = imageUri;
  }
}

export default Post;
